# Тестирование SEO

## ✅ SEO Middleware работает!

Проверено через curl к Express серверу (порт 3001):
- Title заменяется корректно
- Description заменяется корректно
- OG теги заменяются корректно

## ⚠️ Важно: Development vs Production

### В Development (localhost:8080)
- **Vite dev server** отдает HTML напрямую
- SEO middleware **НЕ работает** (это нормально)
- Вы видите плейсхолдеры `__TITLE__` в исходном коде

### В Production
- Express сервер отдает HTML через middleware
- SEO middleware **работает** ✅
- Все плейсхолдеры заменяются на реальные значения

## 🧪 Как проверить SEO прямо сейчас

### Вариант 1: Через curl (быстро)
```bash
curl -H "Accept: text/html" http://localhost:3001/
curl -H "Accept: text/html" http://localhost:3001/about
curl -H "Accept: text/html" http://localhost:3001/blog
```

### Вариант 2: Собрать проект (полная проверка)
```bash
npm run build
npm run server
# Откройте http://localhost:3001/
```

### Вариант 3: Проверить в браузере
1. Откройте http://localhost:3001/ (не 8080!)
2. Правый клик → "Просмотр кода страницы"
3. Проверьте `<title>` и мета-теги

## 📊 Что проверено

✅ SEO данные в БД для всех страниц
✅ Middleware корректно заменяет плейсхолдеры
✅ Автоматическое SEO для статей блога
✅ Fallback на дефолтные значения

## 🚀 В Production всё будет работать автоматически!

