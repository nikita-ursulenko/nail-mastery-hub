# 🚀 Развертывание на сервере

## Быстрый старт

Все файлы для развертывания находятся в папке `deploy/`.

### 📦 Что нужно скопировать на сервер:

1. **Дамп базы данных:**

   - `deploy/database_dump.sql.gz` (26KB) - сжатый дамп
   - или `deploy/database_dump.sql` (137KB) - полный дамп

2. **Резервная копия файлов:**

   - `deploy/uploads_backup_*.tar.gz` (285KB) - загруженные файлы

3. **Скрипты (уже в репозитории):**
   - `deploy/restore_database.sh` - восстановление БД
   - `deploy/create_dump.sh` - создание нового дампа
   - `deploy/backup_uploads.sh` - бэкап файлов

### 📋 Пошаговая инструкция:

1. **Клонируйте репозиторий на сервере:**

   ```bash
   git clone https://github.com/nikita-ursulenko/nail-mastery-hub.git
   cd nail-mastery-hub
   ```

2. **Скопируйте дамп и бэкап файлов в папку `deploy/`:**

   ```bash
   # Через scp или другой способ
   scp database_dump.sql.gz user@server:/path/to/nail-mastery-hub/deploy/
   scp uploads_backup_*.tar.gz user@server:/path/to/nail-mastery-hub/deploy/
   ```

3. **Следуйте инструкциям в `deploy/README.md`**

## 📖 Подробная инструкция

См. файл `deploy/README.md` для полной инструкции по развертыванию.

## 🔄 Обновление дампа

Если нужно обновить дамп БД на локальной машине:

```bash
cd deploy
./create_dump.sh
./backup_uploads.sh
```

Затем скопируйте новые файлы на сервер.

## ⚠️ Важно

- **Не коммитьте дампы и бэкапы в git** - они уже в `.gitignore`
- **Храните дампы в безопасном месте** - они содержат все данные
- **Регулярно создавайте новые дампы** перед важными обновлениями

