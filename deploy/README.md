# Развертывание проекта на сервере

Эта папка содержит все необходимое для развертывания проекта на новом сервере.

## 📦 Содержимое папки

- `database_dump.sql` / `database_dump.sql.gz` - Дамп базы данных PostgreSQL
- `create_dump.sh` - Скрипт для создания нового дампа БД
- `restore_database.sh` - Скрипт для восстановления БД из дампа
- `uploads_backup/` - Резервная копия загруженных файлов (создается отдельно)

## 🚀 Быстрый старт на новом сервере

### 1. Подготовка сервера

```bash
# Установите необходимые инструменты
sudo apt update
sudo apt install -y git docker.io docker-compose nodejs npm

# Клонируйте репозиторий
git clone https://github.com/nikita-ursulenko/nail-mastery-hub.git
cd nail-mastery-hub
```

### 2. Настройка окружения

```bash
# Скопируйте шаблон .env
cp env.template .env

# Отредактируйте .env файл
nano .env
```

**Важно:** Заполните все переменные окружения, особенно:

- `JWT_SECRET` - минимум 32 символа
- `STRIPE_SECRET_KEY` - ваш Stripe секретный ключ
- `STRIPE_WEBHOOK_SECRET` - секрет для webhook
- `DB_PASSWORD` - надежный пароль для БД
- `FRONTEND_URL` - URL вашего сайта (например, https://yourdomain.com)

### 3. Запуск базы данных

```bash
# Запустите PostgreSQL в Docker
docker-compose up -d postgres

# Дождитесь готовности БД (около 10 секунд)
sleep 10
```

### 4. Восстановление базы данных

```bash
# Перейдите в папку deploy
cd deploy

# Восстановите БД из дампа
./restore_database.sh database_dump.sql.gz
# или если дамп не сжат:
./restore_database.sh database_dump.sql
```

### 5. Копирование загруженных файлов

```bash
# Если у вас есть резервная копия uploads
# Скопируйте папку public/uploads на сервер
# Например, через scp:
# scp -r uploads_backup/* user@server:/path/to/nail-mastery-hub/public/uploads/
```

### 6. Установка зависимостей и сборка

```bash
# Вернитесь в корень проекта
cd ..

# Установите зависимости
npm install

# Запустите миграции (на всякий случай)
npm run migrate:up

# Соберите frontend
npm run build
```

### 7. Запуск приложения

#### Вариант 1: Production с PM2 (рекомендуется)

```bash
# Установите PM2
npm install -g pm2

# Запустите сервер
pm2 start npm --name "nail-mastery-server" -- run server

# Настройте автозапуск
pm2 startup
pm2 save
```

#### Вариант 2: Docker Compose (полное развертывание)

См. `docker-compose.prod.yml` для полного развертывания с Nginx.

### 8. Настройка Nginx (опционально)

Создайте конфигурацию Nginx для проксирования запросов:

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    # Frontend
    location / {
        root /path/to/nail-mastery-hub/dist;
        try_files $uri $uri/ /index.html;
    }

    # API
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Uploads
    location /uploads {
        alias /path/to/nail-mastery-hub/public/uploads;
    }
}
```

## 🔄 Создание нового дампа БД

Если нужно обновить дамп БД:

```bash
cd deploy
./create_dump.sh
```

## 📋 Чеклист развертывания

- [ ] Клонирован репозиторий
- [ ] Создан и заполнен `.env` файл
- [ ] Запущен Docker контейнер с PostgreSQL
- [ ] Восстановлена БД из дампа
- [ ] Скопированы файлы из `public/uploads`
- [ ] Установлены зависимости (`npm install`)
- [ ] Выполнены миграции (`npm run migrate:up`)
- [ ] Собран frontend (`npm run build`)
- [ ] Запущен сервер
- [ ] Настроен Nginx (если используется)
- [ ] Настроен SSL сертификат (Let's Encrypt)
- [ ] Проверена работа Stripe webhook

## 🔐 Безопасность

- ✅ Никогда не коммитьте `.env` файл
- ✅ Используйте сильные пароли для БД
- ✅ Настройте firewall (откройте только необходимые порты)
- ✅ Используйте HTTPS (SSL сертификат)
- ✅ Регулярно обновляйте зависимости
- ✅ Делайте резервные копии БД регулярно

## 📞 Поддержка

При возникновении проблем проверьте:

1. Логи сервера: `pm2 logs` или `docker-compose logs`
2. Логи БД: `docker-compose logs postgres`
3. Статус контейнеров: `docker ps`
4. Подключение к БД: `docker exec -it nail-mastery-db psql -U nailmastery -d nail_mastery_hub`

